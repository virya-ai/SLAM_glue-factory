.venv
/build/
*.egg-info
*.pyc
/.idea/
/venv/
/data/
**outputs**
__pycache__
back_data_and models.zip
*.zip
raw_data_slam/
hpatches_results.html
*.pt
*.onnx

GPU
GPU.pub

# Stray artefacts from detached pipeline / inference runs. snapshot_epoch.sh
# commits its output with `git add -f` onto refs/heads/snapshots/<experiment>,
# so ignoring the working-tree copy only keeps `git status` clean; the snapshot
# is still recorded on its own branch.
nohup.out
*.log
latest_keypoints_visualization.png
snapshots/
